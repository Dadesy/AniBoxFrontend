<template>
  <!--
    Skeleton placeholder for TitleCardInner.
    Matches the exact dimensions of a real card (2:3 poster + rounded corners)
    so the grid doesn't shift when real content loads.
    No title/year rows below — TitleCardInner renders those inside the card overlay.
  -->
  <div class="rounded-xl overflow-hidden">
    <div class="aspect-[2/3] w-full bg-cinema-card skeleton-shine" />
  </div>
</template>
